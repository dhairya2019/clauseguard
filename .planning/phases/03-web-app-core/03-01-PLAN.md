---
phase: 03-web-app-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-app/package.json
  - web-app/tsconfig.json
  - web-app/next.config.ts
  - web-app/components.json
  - web-app/src/app/layout.tsx
  - web-app/src/app/page.tsx
  - web-app/src/components/landing/hero.tsx
  - web-app/src/components/landing/how-it-works.tsx
  - web-app/src/components/landing/pricing.tsx
  - web-app/src/components/landing/faq.tsx
  - web-app/src/components/layout/navbar.tsx
  - web-app/src/components/layout/footer.tsx
  - web-app/src/lib/utils.ts
  - web-app/.env.local
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-10

must_haves:
  truths:
    - "Landing page loads at / with hero, how-it-works, pricing, and FAQ sections visible"
    - "All landing sections are Server Components (no 'use client' on landing page)"
    - "Navbar has 'Analyze Contract' CTA linking to /analyze"
    - "Page is responsive — stacks on mobile, horizontal on desktop"
    - "shadcn/ui components (Accordion, Card, Button) are installed and working"
  artifacts:
    - path: "web-app/src/app/page.tsx"
      provides: "Landing page composing all sections"
      contains: "Hero"
    - path: "web-app/src/components/landing/hero.tsx"
      provides: "Hero section with tagline and CTA"
    - path: "web-app/src/components/landing/faq.tsx"
      provides: "FAQ section using shadcn Accordion"
    - path: "web-app/package.json"
      provides: "Next.js project with all dependencies"
      contains: "next"
  key_links:
    - from: "web-app/src/app/page.tsx"
      to: "web-app/src/components/landing/*.tsx"
      via: "import"
      pattern: "import.*landing"
    - from: "web-app/src/app/layout.tsx"
      to: "web-app/src/components/layout/navbar.tsx"
      via: "import"
      pattern: "import.*Navbar"
---

<objective>
Scaffold the Next.js web app and build the complete landing page with all four sections.

Purpose: Establish the web-app project structure with Next.js 15 + Tailwind v4 + shadcn/ui, then build an SEO-friendly landing page that communicates ClauseGuard's value proposition and funnels users to the analysis page.

Output: A working Next.js app at `web-app/` with a fully rendered landing page at `/` and all shadcn/ui components installed for subsequent plans.
</objective>

<execution_context>
@/Users/dhairya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dhairya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-app-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with shadcn/ui and all dependencies</name>
  <files>
    web-app/package.json
    web-app/tsconfig.json
    web-app/next.config.ts
    web-app/components.json
    web-app/src/lib/utils.ts
    web-app/.env.local
  </files>
  <action>
Run these commands from the project root (`/Users/dhairya/ClauseGaurd/`):

1. Create Next.js app:
```bash
npx create-next-app@latest web-app --ts --tailwind --eslint --app --src-dir --import-alias "@/*"
```
Accept all defaults. This creates Next.js 15 with Tailwind v4, App Router, src/ directory.

2. Install AI SDK and Anthropic provider (needed by Plan 03-02, install now to avoid re-scaffolding):
```bash
cd web-app && npm install ai @ai-sdk/react @ai-sdk/anthropic zod
```

3. Initialize shadcn/ui:
```bash
npx shadcn@latest init
```
Accept defaults (New York style, neutral base color).

4. Add all shadcn components needed across plans 01-03:
```bash
npx shadcn@latest add card badge button textarea accordion skeleton separator
```

5. Install lucide-react icons (may already be installed by shadcn):
```bash
npm install lucide-react
```

6. Create `web-app/.env.local` with placeholder:
```
ANTHROPIC_API_KEY=your-api-key-here
```

Do NOT modify `next.config.ts` for cross-package imports -- we are copying shared code per research recommendation (simpler than path aliases across packages).
  </action>
  <verify>
```bash
cd /Users/dhairya/ClauseGaurd/web-app && npm run build
```
Build succeeds with no errors. Verify `node_modules/ai`, `node_modules/@ai-sdk/react`, `node_modules/@ai-sdk/anthropic` exist.
  </verify>
  <done>Next.js 15 app exists at web-app/ with Tailwind, shadcn/ui initialized, all dependencies installed, and build passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing page with hero, how-it-works, pricing, and FAQ sections</name>
  <files>
    web-app/src/app/layout.tsx
    web-app/src/app/page.tsx
    web-app/src/components/landing/hero.tsx
    web-app/src/components/landing/how-it-works.tsx
    web-app/src/components/landing/pricing.tsx
    web-app/src/components/landing/faq.tsx
    web-app/src/components/layout/navbar.tsx
    web-app/src/components/layout/footer.tsx
  </files>
  <action>
All landing page components MUST be Server Components (no "use client"). Only the FAQ accordion needs interactivity, but shadcn Accordion works as a Server Component since Radix handles client-side via its own internal "use client" boundary.

**web-app/src/components/layout/navbar.tsx:**
- Sticky top navbar with ClauseGuard logo (text, no image), nav links (How It Works, Pricing, FAQ as anchor links #how-it-works, #pricing, #faq)
- Right side: "Analyze Contract" Button (shadcn Button) linking to `/analyze`
- Use `next/link` for navigation
- Responsive: hamburger menu on mobile is NOT needed for MVP -- just show logo + CTA button on mobile, hide nav links

**web-app/src/components/layout/footer.tsx:**
- Simple footer: "ClauseGuard - AI Contract Analysis for Indian Law" + disclaimer text + copyright
- Keep minimal

**web-app/src/app/layout.tsx:**
- Import and use `next/font/google` with Inter font
- Include Navbar and Footer wrapping `{children}`
- Set metadata: title "ClauseGuard - AI Contract Analysis for Indian Law", description about pasting contracts and getting instant analysis

**web-app/src/components/landing/hero.tsx:**
- Large headline: "Understand Your Contracts in Minutes, Not Hours"
- Subheading: "Paste any contract and get instant clause-by-clause risk analysis with India-specific legal awareness. Powered by AI."
- CTA button: "Analyze a Contract Free" linking to `/analyze` (shadcn Button, large size)
- Secondary text: "No signup required"
- Use Shield icon from lucide-react as visual element
- Centered layout, generous padding (py-20 lg:py-32)

**web-app/src/components/landing/how-it-works.tsx:**
- Section id="how-it-works"
- 3-step cards using shadcn Card:
  1. "Paste Your Contract" - FileText icon - "Copy and paste your contract text into the analysis box"
  2. "AI Analyzes Every Clause" - Search icon - "Our AI reviews each clause for risks, missing protections, and India-specific legal issues"
  3. "Get Actionable Results" - CheckCircle icon - "See color-coded risk levels with plain English explanations and safer alternatives"
- Grid layout: 3 columns on lg, 1 on mobile

**web-app/src/components/landing/pricing.tsx:**
- Section id="pricing"
- Two cards: Free tier and Pro tier (even though Pro is Phase 4, show it as "Coming Soon")
- Free: "3 analyses/month", "Full clause-by-clause breakdown", "India-specific legal flags", "Safer alternative suggestions" -- Button: "Start Free"
- Pro: "Unlimited analyses", "Priority processing", "Export reports" -- Button: "Coming Soon" (disabled)
- Use shadcn Card with slight shadow differentiation

**web-app/src/components/landing/faq.tsx:**
- Section id="faq"
- Use shadcn Accordion component
- Questions:
  1. "Is ClauseGuard a replacement for a lawyer?" -> "No. ClauseGuard provides informational analysis to help you understand contracts better. For important agreements, always consult a qualified advocate."
  2. "What types of contracts can I analyze?" -> "Freelance agreements, NDAs, employment contracts, SaaS agreements, vendor contracts, and terms of service."
  3. "How does India-specific analysis work?" -> "Our AI is trained on Indian Contract Act (S.27, S.28, S.25), FEMA regulations, Copyright Act, and Arbitration Act provisions to flag India-specific risks."
  4. "Is my contract data stored?" -> "No. Contract text is sent directly to the AI for analysis and is not stored on our servers."
  5. "How accurate is the analysis?" -> "ClauseGuard identifies common contractual risks with high accuracy, but complex legal situations require professional legal counsel."

**web-app/src/app/page.tsx:**
- Server Component (NO "use client")
- Import and compose: Hero, HowItWorks, Pricing, FAQ
- Export metadata for SEO: title, description, openGraph

All components use Tailwind utility classes. Use `max-w-6xl mx-auto px-4` for content width. Sections separated by `py-16 lg:py-24`. Use neutral/professional colors -- no bright colors except the green CTA button.
  </action>
  <verify>
```bash
cd /Users/dhairya/ClauseGaurd/web-app && npm run build && npm run start &
sleep 3 && curl -s http://localhost:3000 | grep -c "ClauseGuard"
kill %1 2>/dev/null
```
Build succeeds. The landing page HTML contains "ClauseGuard" text. Verify no "use client" directive in page.tsx or any landing/* component files:
```bash
grep -r "use client" /Users/dhairya/ClauseGaurd/web-app/src/components/landing/ /Users/dhairya/ClauseGaurd/web-app/src/app/page.tsx; echo "Exit: $?"
```
Should find zero matches (exit 1 from grep = no matches = correct).
  </verify>
  <done>Landing page at / renders with hero (headline + CTA), how-it-works (3 step cards), pricing (free + pro cards), and FAQ (5 questions in accordion). All sections are Server Components. Navbar has "Analyze Contract" CTA linking to /analyze. Page is responsive.</done>
</task>

</tasks>

<verification>
1. `cd web-app && npm run build` completes without errors
2. Landing page at localhost:3000 shows all 4 sections
3. No "use client" in landing page components (SSR for SEO)
4. "Analyze Contract" button links to /analyze
5. Responsive layout works (3-col grid on lg, stacked on mobile)
</verification>

<success_criteria>
- Next.js 15 project exists at web-app/ with Tailwind, shadcn/ui, AI SDK dependencies
- Landing page renders hero, how-it-works, pricing, FAQ as Server Components
- Build passes cleanly with zero errors
- All shadcn components (Card, Badge, Button, Textarea, Accordion, Skeleton, Separator) installed
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-app-core/03-01-SUMMARY.md`
</output>
